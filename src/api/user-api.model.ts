export type ProfileEditData = {
  firstName: string,
  secondName: string,
  displayName: string,
  login: string,
  email: string,
  phone: string
}

export type ProfileChangePasswordData = {
  oldPassword: string,
  newPassword: string
}
