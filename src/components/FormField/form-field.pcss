.form-field {
  &:not(:first-of-type) {
    margin-top: 16px;
  }

  &__input-label {
    display: inline-block;
    width: 100%;

    margin-bottom: 8px;

    color: var(--color-grey);

    font-size: 9px;
    line-height: 9px;
  }
}
