{{#if isDisabled}}
    <button type="submit" class="{{styles.button_disabled}} {{styles.button}}" disabled>{{label}}</button>
{{else}}
    <button type="submit" class="{{styles.button}}">{{label}}</button>
{{/if}}