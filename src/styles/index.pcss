@import "reset.pcss";
@import "variables.pcss";

html {
  font-family: "Inter", sans-serif;
  color: var(--color-black-light);
}

:global {
  .centered {
    display: flex;

    justify-content: center;
    align-items: center;
  }

  .full-screen {
    width: 100%;
    height: 100vh;
  }
}
